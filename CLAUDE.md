# このリポジトリの扱い

## 成果物の受け渡し（重要）

伊佐さんの手元の作業フォルダは次の場所。

```
/Users/y.isa/Desktop/Claude code/pharmacy_rx_predictor
```

**Mac上でClaude Codeを起動している場合**は、そこが作業ディレクトリなのでファイルは直接更新される。

**Web版（クラウド実行）のセッションの場合**は、リポジトリがクラウド側のコンテナに
クローンされているため、上記フォルダには直接書き込めない。この場合は必ず次の両方を行う。

1. 変更をブランチへコミット＆プッシュする
2. 更新した主要ファイルを `SendUserFile` でチャットに出す（伊佐さんがダウンロードして
   上記フォルダに置く運用のため）

「ファイルを更新しておいた」とだけ伝えて終わらせない。クラウド実行時は、手元のフォルダには
何も起きていないため。

## このリポジトリの中身

薬局の出店候補地を分析するStreamlitツール群。すべて厚労省「医療情報ネット（ナビィ）」を
スクレイピングして、商圏内の医療機関・調剤薬局を収集する。

| ファイル | 用途 |
|---|---|
| `pharmacy_rx_predictor/薬局出店分析ツール_v1.4.py` | **現行版**。出店候補地ごとの処方箋獲得予測 |
| `pharmacy_rx_predictor/pharmacy_area_finder.py` | 商圏内の調剤薬局リストアップ |
| `pharmacy_rx_predictor/area_analysis.py` | 商圏内の医療機関＋薬局の統合リストアップ |
| `pharmacy_rx_predictor/navii_health_check.py` | ナビィ側のHTML構造が変わっていないかを実サイトで確認する |
| `pharmacy_rx_predictor/tests/` | 通信不要の自動テスト |

`薬局出店分析ツール_v1.0〜v1.3` と `app*.py` は旧版。触らない。

## 変更したら必ず通すもの

```bash
python3 pharmacy_rx_predictor/tests/test_parsers.py
python3 pharmacy_rx_predictor/tests/test_e2e.py
```

ネットワーク不要。ナビィを模したフェイクサーバ相手に収集処理を通しで実行し、
取りこぼしが起きていないかを確認する。

## 過去に起きた不具合と、その原因（同じ轍を踏まないために）

1. **ナビィのHTML変更で全ツールが0件になった**（2026-07）
   施設名の見出しが `<h3 class="name">` → `<h2 class="name">` に変わった。検索リクエスト
   自体は成功するため「動いているのに何も出ない」という壊れ方をする。
   → h2/h3両対応＋「kikanCdを含むリンクを直接探す」フォールバックで対応済み。
   再発したら `navii_health_check.py` を実行すれば、どの前提が崩れたか分かる。

2. **静かな打ち切りで件数が減っていた**
   医療機関の詳細取得が `[:50]` 固定、一覧のページ数も固定、総件数の読み取りが
   「本文で最初に出るN件」だった（新HTMLの「20件表示」を総件数と誤読）。
   → **件数に上限を入れるときは、必ず画面に警告を出すこと。黙って減らさない。**

3. **重複排除で別の施設まで消していた**
   `name_similarity`（文字集合の重なり率）0.65以上を同一施設としていたため、
   「田中内科クリニック」と「中田内科クリニック」（類似度1.00）が消えていた。
   → 機関コード（kikanCd）優先の `same_facility()` を使う。
   **`name_similarity` を同一判定に使わないこと。**

## 依存ライブラリ

`pharmacy_rx_predictor/requirements-container.txt` に動作確認済みの版数を固定してある。
`requirements.txt` は `>=` 指定なので、再構築時に破壊的変更が入る可能性がある。
版数を上げるときは上記テストを通してから。
